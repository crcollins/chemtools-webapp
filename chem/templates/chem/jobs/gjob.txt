#!/bin/bash
#PBS -l nodes={{ nodes }}
#PBS -l walltime={{ time }}
#PBS -o {{ name }}.out
#PBS -e {{ name }}.err
#PBS -m bea
#PBS -M {{ email }}
#PBS -A TG-CHE120020
#PBS -V

name={{ name }}
base=$HOME{% if internal %}/chemtools/{% endif %}
temp=/oasis/scratch/$USER/temp_project/

cp $base/$name.gjf $temp
export GAUSS_SCRDIR=/oasis/scratch/$USER/temp_project/
. /etc/profile.d/modules.sh
module load gaussian
exe=`which g09`
/usr/bin/time $exe < $temp/$name.gjf > $temp/$name.log

mv $temp/$name.* $base/done/

